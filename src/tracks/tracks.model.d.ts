export interface trackCreationDTO {
    called: number;
    unCalled: number;
    desc: string;
    date: Date;
}

export interface trackDTO {
    id: number;
    called: number;
    unCalled: number;
    desc: string;
    date: Date;
}