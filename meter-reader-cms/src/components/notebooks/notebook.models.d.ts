export interface notebookDTO {
  id: number;
  number: number;
  tracksCount: number
}

export interface popularNotebookDTO {
  id: number;
  number: number;
  desc: number;
  numberOfRaces: number;
}